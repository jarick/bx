build_settings:
    verbose: false
    prefer_symlink: false
    ignore: 
        - "vendor"
        - "spec"
        - "test"

setup:
    composer:
        action: "install"
        
test:
    #php_code_sniffer:
    #    standard: "PSR2"
    php_unit:
        directory: "test/BX"
        args: "--bootstrap 'test/bootstrap.php' --configuration 'test/phpunit.xml'"
    php_spec:
        directory: ""
complete:
